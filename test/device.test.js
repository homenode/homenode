const chai = require('chai');
const chaiDeepEqual = require('chai-shallow-deep-equal');

chai.use(chaiDeepEqual);
const { assert } = chai;

describe('Device', function _deviceTests() {
  it('should create a new instance', function() {

  });
});
